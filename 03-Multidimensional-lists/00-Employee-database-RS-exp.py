employee_database = []
data = {'ID': '---', 'Name': '---', 'Age': '---'}
for i in range(3):
    employee_database.append([])
    for j in range(len(employee_database)):
        employee_database[j].append(data)
print(employee_database)
